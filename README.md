Find duplicate files.  Supply a list of directories you want to recursively search as command arguments.

Example:
$ python finddup.py ~/pictures ./ ../pictures2
$ perl finddup.pl ~/pictures ./ ../pictures2

